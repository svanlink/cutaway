# Generated by xcodegen — regenerate with `xcodegen generate`
Cutaway.xcodeproj/

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Local design working files stay; heavy exports don't
*.icns.tmp

# coverage droppings from a local test run
default.profraw

# scheduler run state — churns on its own, belongs to the machine not the repo.
# The six department charters under .claude/agents/ DO stay tracked.
.claude/scheduled_tasks.lock

# graphify knowledge graph — derived from the source tree, regenerate with `graphify .`
graphify-out/

# stray build artefacts — a bare `swiftc file.swift` drops `main` here
main
.build/
